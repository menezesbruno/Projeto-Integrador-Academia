@page
@model Academia.Pages.FormasPagamento.DeleteModel

@{
    ViewData["Title"] = "Apagar";
}

<partial name="_Breadcrumbs" />

<div class="container py-5">
    <h3>Você tem certeza que quer remover isto?</h3>
    <div>
        <h4>FORMA DE PAGAMENTO</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.FormaPagamento.Nome)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.FormaPagamento.Nome)
            </dd>
        </dl>

        <form method="post">
            <input type="hidden" asp-for="FormaPagamento.Id" />
            <input type="submit" value="Apagar" class="btn btn-danger" /> |
            <a asp-page="./Index">Voltar para a lista</a>
        </form>
    </div>
</div>